language: ruby
sudo: true
rvm:
  - 2.4.1
cache: bundler
before_install:
  - sudo apt-get install -y imagemagick
before_script:
  - bundle exec rake db:setup
script:
  - bundle exec rake test
services:
  - redis-server
addons:
  postgresql: "9.5"
  apt:
    update: true
  chrome: stable