<p><%= t("common.greeting") %>,</p>

<%# TODO: add translations %>

<p>
  <div>
    The requested <%= @export.category %> export is available. You can download it by clicking on the link below.
  </div>
  <br>
  <div>
    <%= link_to "Download export (validity: " + distance_of_time_in_words(@export.created_at, @export.created_at + @export.validity) + ")",
                download_catalog_admin_export_path(@export.catalog.slug, I18n.locale, @export.id)
    %>
  </div>
</p>

<p>
  <div>
    Your can view the logs of all the requested exports for the "<%= @export.catalog.name %>" catalog
    <%= link_to "here", catalog_admin_exports_path(@export.catalog.slug, I18n.locale) %>.
  </div>
</p>
