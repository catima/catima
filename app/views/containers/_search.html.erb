<%= container.content['description'].html_safe %>

<% params[:style] = container.content['display_type'] %>
<%= render :partial => partial_name, :locals => { :search_results => results, :saved_search => saved_search } %>
