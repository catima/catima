<% provide(:title) do %>
  Items matching: <%= @search.query %>
<% end %>

<%= render(
      "shared/search/results_list",
      :items => @search.items,
      :offset => @search.offset
    ) %>
<%= paginate(@search.items) %>
