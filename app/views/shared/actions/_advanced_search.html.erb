<% if policy(model).edit? %>
  <%= link_to(
          fa_icon(:pencil),
          edit_catalog_admin_advanced_search_configuration_path(model.catalog.slug, I18n.locale, model.id),
          class: "btn btn-xs btn-outline-secondary menu-action-edit",
          "data-toggle" => "tooltip", "data-placement" => "top", "title" => t("edit")
      ) %>
<% end %>
<% if policy(model).destroy? %>
  <%= link_to(
          fa_icon(:trash),
          catalog_admin_advanced_search_configuration_path(model.catalog.slug, I18n.locale, model.id),
          :method => :delete,
          :data => {
              :confirm => t("delete_confirm", {model: model.title})
          },
          class: "btn btn-xs btn-outline-secondary menu-action-delete",
          "data-toggle" => "tooltip", "data-placement" => "top", "title" => t("delete")
      ) %>
<% end %>
