<% if policy(model).download? %>
  <%= link_to(
          fa_icon(:download),
          download_catalog_admin_export_path(model.catalog.slug, I18n.locale, model.id),
          class: "btn btn-sm btn-outline-secondary export-action-download",
          "data-toggle" => "tooltip", "data-placement" => "top", "title" => t("download")
      ) %>
<% end %>
