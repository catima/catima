<%= react_component('GeoViewer/components/GeoViewer', {
      features: features.to_json,
      layers: layers,
      zoomLevel: zoom_level.to_i,
      catalog: @catalog.slug,
      locale: I18n.locale
    })
%>
