<%= f.viim_datetime_select(
      "after",
      :label => t(".after"),
      :format => field.format
      ) %>
<%= f.viim_datetime_select(
      "before",
      :label => t(".before"),
      :format => field.format
      ) %>
