<p>
  <%= link_to(
        fa_icon(:"chevron-circle-left", :text => t(".back")),
        search_path
        ) %>

  <span class="pull-right">
    <% if nav.previous %>
      <%= item_list_link(search, nav.previous, nav.previous_offset) do %>
        <%= fa_icon(:"chevron-left") %>
        <%= t(".previous", :item => nav.previous.display_name) %>
      <% end %>
    <% end %>
    &nbsp;&nbsp;
    <% if nav.next %>
      <%= item_list_link(search, nav.next, nav.next_offset) do %>
        <%= t(".next", :item => nav.next.display_name) %>
        <%= fa_icon(:"chevron-right") %>
      <% end %>
    <% elsif nav.previous %>
      <%= t(".no_more") %>
    <% end %>
  </span>
</p>
