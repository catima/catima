<% provide(:title, item_type.name_plural) %>

<%# TODO: customize page title based on field being browsed? %>

<%= render("shared/search/results_list", :item_list => @browse) %>
<%= paginate(@browse.items) %>
