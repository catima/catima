<h1>New database element</h1>
<hr>
<p id="notice"><%= notice %></p>

<%= form_for([@instance, @schema_element], url:instance_schema_elements_path(@instance), html:{ class:'form-horizontal' }) do |f| %>
  <%= render 'form', f: f %>
<% end %>

<%= link_to '< Back', edit_instance_path(@instance) %>
