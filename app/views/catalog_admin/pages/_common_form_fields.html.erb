<% if catalog.valid_locales.many? %>
  <%= locale_language_select(
        f,
        :locale,
        :locales => catalog.valid_locales,
        :label => "Language") %>
<% end %>

<%= f.text_field(:slug, :help => slug_help_text, :autofocus => true) %>
<%= f.text_field(:title) %>
<%= summernote_text_area(f, :content) %>
