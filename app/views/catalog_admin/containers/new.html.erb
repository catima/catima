<% provide(:title, t('.new_container', container: @container.type_name)) %>

<div class="panel-body">
  <%= bootstrap_form_for(
        @container,
        :as => :container,
        :url => { :action => :index }
        ) do |f| %>
    <%= render("common_form_fields", :f => f) %>
  <% end %>
</div>
