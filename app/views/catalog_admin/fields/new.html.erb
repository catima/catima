<% provide(:title, "New #{@field.type_name.downcase} field") %>

<%= bootstrap_form_for(
      @field,
      :as => :field,
      :url => catalog_admin_item_type_fields_path(catalog, @item_type)
      ) do |f| %>
  <%= render("common_form_fields", :f => f) %>
<% end %>
