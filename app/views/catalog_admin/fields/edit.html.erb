<% provide(:title, "Edit #{@item_type.name} “#{@field.name}” field") %>

<%= bootstrap_form_for(
      @field,
      :as => :field,
      :url => catalog_admin_item_type_field_path(catalog, @item_type, @field)
      ) do |f| %>
  <%= render("common_form_fields", :f => f) %>
<% end %>
