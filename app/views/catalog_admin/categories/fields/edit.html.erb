<% provide(:title, "Edit #{@category.name} “#{@field.name}” field") %>

<%= bootstrap_form_for(
      @field,
      :as => :field,
      :url => catalog_admin_category_field_path(catalog, @category, @field)
      ) do |f| %>
  <%= render("common_form_fields", :f => f) %>
<% end %>
