<% provide(:title, "Edit choice set") %>

<div class="panel-body">
  <%= bootstrap_form_for(
        @choice_set,
        :url => catalog_admin_choice_set_path) do |f| %>

    <%= render("common_form_fields", :f => f) %>

    <%= f.primary("Update choice set") %>
    <%= link_to(
          "Cancel",
          catalog_admin_choice_sets_path,
          :class => "btn btn-link") %>

  <% end %>
</div>
