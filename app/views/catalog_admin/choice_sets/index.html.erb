<% provide(:title, t(".choice_sets")) %>

<% provide(:actions) do %>
  <%= link_to(t(".import"), new_import_catalog_admin_choice_sets_path(@catalog, I18n.locale), class: 'mr-4') %>
  <%= link_to(new_catalog_admin_choice_set_path) do %>
    <%= fa_icon(:"plus-square", :text => t('.new_choice_set')) %>
  <% end %>
<% end %>

<table class="table">
  <thead>
    <tr>
      <th><%= t('.name') %></th>
      <th><%= t('.status') %></th>
      <th><%= t('.choices') %></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @choice_sets.each do |set| %>
      <tr>
        <td><%= set.name %></td>
        <td><%= deactivation_status_label(set) %></td>
        <td><%= choice_set_abbreviated_choices(set) %></td>
        <td align="right" class="whitespace-nowrap">
          <%= render "shared/actions/choiceset", :model => set %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
