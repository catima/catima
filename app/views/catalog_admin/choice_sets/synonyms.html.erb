<% provide(:title, t(".title", :choice_set_name => @choice_set.name)) %>

<div class="panel-body">
  <%= bootstrap_form_for(
    @choice_set,
    :url => catalog_admin_choice_set_update_synonyms_path) do |f| %>

      <div class="row">
        <div class="col-md-12">
          <%=
              react_component('ChoiceSynonymEditorContainer',
                props: {
                  choices: @choice_set.search_data_as_hash,
                  synonyms: @choice_set.synonyms,
                  available_languages: @choice_set.catalog.valid_locales,
                  add_synonym_label: t(".add"),
                  select_placeholder: t(".choice_placeholder")
                },
                prerender: false)
          %>
        </div>
      </div>

      <div class="col-md-12">
          <%= f.primary(t(".save")) %>
          <%= link_to(
                t("cancel"),
                edit_catalog_admin_choice_set_path(:id => @choice_set.id),
                :class => "btn btn-link") %>
      </div>
  <% end %>
</div>
