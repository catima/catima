<div class="nested-fields">
  <%= locale_form_group(f, :short_name, :text_field) %>
  <%= locale_form_group(f, :long_name, :text_field) %>
  <%= link_to_remove_association(
        fa_icon(:trash, :text => "Remove choice"),
        f) %>
</div>
