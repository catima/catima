<% provide(:title, "Edit item view") %>

<%= bootstrap_form_for(@item_view, url: catalog_admin_item_view_path) do |f| %>
  <%= render("common_form_fields", :f => f) %>
  <%= f.primary("Update item view") %>
  <%= link_to(
        t('cancel'),
        catalog_admin_item_type_fields_path,
        :class => "btn btn-link") %>
<% end %>
