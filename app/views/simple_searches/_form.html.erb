<%= bootstrap_form_tag(
      :url => { :controller => controller, :action => action, :uuid => '' },
      :method => "POST",
      :layout => :inline,
      :html => { :class => "justify-content-center my-4" }) do |f| %>

    <div class="clearable-input mr-sm-2">
      <div class="input-group">
        <%= f.search_field(
              :q,
              :value => @saved_search&.query,
              :label => t('simple_searches.form.submit'),
              :placeholder => @item_type.present? ? t('.placeholder', :item_type => @item_type.name_plural) : t('.submit'),
              :hide_label => true,
              :autofocus => true,
              :class => "form-control",
              :style => "width: 25em"
              ) %>

        <div class="input-group-addon">
          <% unless params[:q].nil? %>
            <a href="<%= request.path.remove('search', '') %>" class="clear-input"><i class="fa fa-times"></i></a>
          <% end %>
        </div>
      </div>
    </div>

    <%= f.submit(t(".submit"), class: 'btn btn-outline-secondary mr-sm-2') %>

    <% if display_advanced_search %>
      <%= link_to(
            t(".advanced"),
            new_advanced_search_path,
            :class => "btn btn-link") %>
    <% end %>
<% end %>
