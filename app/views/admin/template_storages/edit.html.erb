<% provide(:title, "Edit template") %>

<%= bootstrap_form_for([:admin, @template_storage]) do |f| %>

  <%= render("common_form_fields", :f => f) %>

  <hr>

  <%= f.primary("Save template") %>
  <%= link_to "Back", admin_dashboard_path, :class => "btn btn-link" %>

<% end %>
