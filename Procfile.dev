# Launch Puma web server
web: rails s -p 3000

# - Generate js locales
# - Watch process with webpack to compile the changed files
client: bundle exec rake react_on_rails:locale && sh -c 'rm -rf public/packs/* || true && bin/webpack -w'

# Start the Sidekiq background job processor
worker: bundle exec sidekiq
