# == Schema Information
#
# Table name: suggestions
#
#  id           :bigint           not null, primary key
#  content      :text
#  processed_at :datetime
#  created_at   :datetime         not null
#  updated_at   :datetime         not null
#  catalog_id   :bigint           not null
#  item_id      :bigint           not null
#  item_type_id :bigint           not null
#  user_id      :bigint
#
# Indexes
#
#  index_suggestions_on_catalog_id    (catalog_id)
#  index_suggestions_on_item_id       (item_id)
#  index_suggestions_on_item_type_id  (item_type_id)
#  index_suggestions_on_user_id       (user_id)
#
# Foreign Keys
#
#  fk_rails_...  (catalog_id => catalogs.id)
#  fk_rails_...  (item_id => items.id)
#  fk_rails_...  (item_type_id => item_types.id)
#  fk_rails_...  (user_id => users.id)
#
one_comment1:
  catalog: one
  item_type: one
  item: one_author_stephen_king
  user: :one_user_deleted
  content: existing_suggestion1

two_comment1:
  catalog: two
  item_type: two_author
  item: two
  user: two_user
  content: existing_suggestion1

two_comment2:
  catalog: two
  item_type: two_author
  item: two
  user: two
  content: existing_suggestion2
